import 'package:flutter/material.dart';

/// Paleta oficial do app (mesmo padrao do passageiro)
class AppColors {
  static const Color primary = Color(0xFFF7931E);
  static const Color background = Color(0xFF0E2A3B);
  static const Color surface = Color(0xFF122C3A);
  static const Color accent = Color(0xFF1E3A50);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textTertiary = Colors.white38;

  static const Color success = Color(0xFF27AE60);
  static const Color error = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);

  static const Color disabled = Colors.white24;
  static const Color overlay = Colors.black54;

  const AppColors._();
}
